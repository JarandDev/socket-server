name: Docker

on:
  workflow_dispatch:
    inputs:
      oldTag:
        description: "Old tag"
        type: string
        required: true
      newTag:
        description: "New tag"
        type: string
        required: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  OLD_TAG: ${{ inputs.oldTag }}
  NEW_TAG: ${{ inputs.newTag }}

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write

    steps:

      - name: Log into registry ${{ env.REGISTRY }}
        uses: docker/login-action@5e57cd118135c172c3672efd75eb46360885c0ef # v3.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag Docker image
        run: |
          docker pull ghcr.io/jaranddev/socket-server@${{ env.OLD_TAG }}
          docker tag ghcr.io/jaranddev/socket-server@${{ env.OLD_TAG }} ghcr.io/jaranddev/socket-server:${{ env.NEW_TAG }}
          docker push ghcr.io/jaranddev/socket-server:${{ env.NEW_TAG }}
